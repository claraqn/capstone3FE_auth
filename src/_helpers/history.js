import { createBrowserHistory } from 'history';

export const history = createBrowserHistory();

// import { createBrowerHistory } from '../../../node_modules/history';
// export const history = createBrowerHistory();
